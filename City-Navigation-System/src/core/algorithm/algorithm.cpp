#include<iostream>
#include"../../api/NavigationAPI.h"
#include<vector>
#include<string>

PathResult calculate_shortest_path(int start_id, int end_id, bool consider_traffic)
{
    if(!consider_traffic)
    {
        //算最短距离对应的路
    }
    else
    {
        //算考虑了交通情况从A到B的通行最短时间对应的路
    }
}