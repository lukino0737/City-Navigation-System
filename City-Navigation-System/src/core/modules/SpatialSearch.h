#pragma once

#include <vector>
#include "../../api/NavigationAPI.h"

class SpatialSearch {
public:
    SpatialSearch();
    ~SpatialSearch();
    //std::vector<Node> getNearestPoints(double x, double y, int count);
};
